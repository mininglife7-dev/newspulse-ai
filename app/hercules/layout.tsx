/**
 * HERCULES Layout — Founder's control surfaces
 */

export default function HerculesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      {children}
    </div>
  );
}
