# UI Defect Register — NewsPulse AI

**Audit date:** 2026-07-09 · **Status legend:** FIXED = corrected and verified in this audit.

| #    | Severity     | Location                                                     | Defect                                                                                                                                                                                                                                                                                         | Status                                                                                                                                                                                  |
| ---- | ------------ | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01 | **Critical** | `lib/supabase.ts`                                            | Browser Supabase client was created eagerly at module load with `!` assertions and was **never used by any code path**. Any `next build` or server boot without env vars crashed (`Failed to collect page data for /api/history/[id]`).                                                        | FIXED — lazy `getSupabasePublic()`; zero-env build verified green.                                                                                                                      |
| D-02 | High         | `app/history/page.tsx`                                       | `/history/[id]` detail page was **orphaned** — no link anywhere in the UI navigated to it.                                                                                                                                                                                                     | FIXED — keyword in each history row links to its detail page.                                                                                                                           |
| D-03 | High         | `lib/supabase.ts` → `/api/history` → history screen          | DB failures were swallowed and returned as an empty list, so the UI showed **"No searches yet"** during an outage (false confidence; Founder Trust violation).                                                                                                                                 | FIXED — `getSearchHistory` throws, API returns 500 + error, UI shows the error banner. Smoke-tested.                                                                                    |
| D-04 | High         | `app/history/[id]/page.tsx`                                  | DB failures returned `null`, rendering a **false 404** ("Page not found") when the truth was "database unreachable".                                                                                                                                                                           | FIXED — DB errors throw to the error boundary; malformed UUIDs (Postgres `22P02`) remain genuine 404s. Smoke-tested.                                                                    |
| D-05 | Medium       | `app/api/history/[id]/route.ts`                              | `DELETE /api/history/:id` existed but **no button used it** — a dead endpoint / missing workflow.                                                                                                                                                                                              | FIXED — per-row Delete button (confirm dialog, spinner, error alert) on the history table.                                                                                              |
| D-06 | Medium       | `app/page.tsx`                                               | Results header displayed the **live input text**, not the searched query: type something new without searching and the screen said "8 results for \"xyz\"" for results that belonged to another query.                                                                                         | FIXED — header bound to `lastQuery`, set only when a search actually runs.                                                                                                              |
| D-07 | Medium       | `app/page.tsx`, `app/history/[id]/page.tsx`, `lib/openai.ts` | `gpt-4o-mini` hard-coded in 3 places — screens could silently drift from the model actually used.                                                                                                                                                                                              | FIXED — single `SUMMARY_MODEL` constant in `lib/constants.ts`.                                                                                                                          |
| D-08 | Medium       | `app/history/[id]/page.tsx`                                  | Timestamp rendered server-side in the **server's timezone**, contradicting the history table which formats client-side in the visitor's timezone. Formatting logic was also duplicated inline.                                                                                                 | FIXED — shared `LocalDateTime` client component using `formatAbsoluteDate`.                                                                                                             |
| D-09 | Low          | `app/history/page.tsx`, `app/history/[id]/page.tsx`          | Count badge showed stored `result_count` while the screen rendered the `results` array — two sources for one metric. Also: suggestion chips filled the input but didn't search; "no results" was styled as a red **error**; Re-run action used an `ExternalLink` icon for internal navigation. | FIXED — counts derived from the rendered array; suggestion chips run the search; no-results renders a neutral `EmptyState`; Re-run uses the `RotateCw` icon (matching the detail page). |
| D-11 | High         | `.github/workflows/ci.yml`                                   | CI runs `npm ci`, which **requires a lockfile**, but no `package-lock.json` was committed — the CI pipeline could never pass.                                                                                                                                                                  | FIXED — lockfile committed; smoke-test step added to CI.                                                                                                                                |
| D-10 | Low          | `README.md`, `app/layout.tsx`                                | README embedded two screenshots from a `public/screenshots/` directory that doesn't exist (broken images on GitHub). Build warned about missing `metadataBase` (OG images resolved against localhost).                                                                                         | FIXED — broken image table removed until screenshots exist; `metadataBase` set from `NEXT_PUBLIC_SITE_URL`.                                                                             |

## Second-wave findings (2026-07-10 deep pass)

| #    | Severity | Location                                            | Defect                                                                                                                                                                                                                                                                                        | Status                                                                                                                                                                                                                                    |
| ---- | -------- | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-12 | Medium   | `app/api/health/route.ts`                           | Health reported **degraded** when `NEXT_PUBLIC_SUPABASE_ANON_KEY` was missing, but no code path uses that key — a false alarm that would page an operator over a credential the app never reads.                                                                                              | FIXED — only load-bearing credentials affect the verdict; the anon key is reported informationally (`optional.supabase_anon`). `check-env` and all env docs updated to match.                                                             |
| D-13 | Medium   | `app/api/history/[id]/route.ts`                     | `DELETE` on a nonexistent id returned `ok: true` — false success for a no-op.                                                                                                                                                                                                                 | FIXED — delete-returning is checked; missing rows now get 404.                                                                                                                                                                            |
| D-14 | Low      | `lib/supabase.ts`                                   | `getSupabasePublic` (introduced in wave 1 to replace the crashing eager client) was itself never called — dead code by the audit's own standard.                                                                                                                                              | FIXED — removed; the server-only access pattern is documented in place.                                                                                                                                                                   |
| D-15 | Low      | `lib/supabase.ts` `clearAllHistory`                 | Count-then-delete race: rows inserted between the two queries made the reported `deleted` count wrong.                                                                                                                                                                                        | FIXED — single atomic `delete ... select` returns the exact removed rows.                                                                                                                                                                 |
| D-16 | Low      | `middleware.ts`                                     | Rate-limit bucket map grew without bound (one entry per distinct IP until cold start).                                                                                                                                                                                                        | FIXED — expired buckets are swept once the map exceeds 1000 entries.                                                                                                                                                                      |
| D-17 | Low      | a11y (multiple files)                               | No skip link; search input unlabeled (placeholder only); dynamic error/status banners not announced to screen readers; expand toggles missing `aria-expanded`; no visible keyboard focus outline; animations ignored `prefers-reduced-motion`.                                                | FIXED — see the accessibility pass in commit history; skip-link and announcement behavior are e2e-tested.                                                                                                                                 |
| D-18 | Info     | `supabase/schema.sql`                               | `result_count` was an unconstrained duplicate of `jsonb_array_length(results)` — drift was possible in principle.                                                                                                                                                                             | FIXED — retained as a constraint-checked cached derivation; decision + Postgres-validated migration in `docs/decisions/0001-result-count.md`.                                                                                             |
| D-19 | Medium   | `app/api/search/route.ts`                           | Keyword accepted with no type or length validation: a non-string keyword threw an unhandled TypeError (opaque 500) and an arbitrarily long keyword was forwarded to Firecrawl.                                                                                                                | FIXED — non-string → 400, > 200 chars → 400; input `maxLength` mirrors the limit; smoke-tested. The previously untested 429 rate-limit path is now smoke-tested too.                                                                      |
| D-20 | Medium   | `app/layout.tsx`, `app/robots.ts`, `app/sitemap.ts` | The canonical site URL was resolved three different ways (layout fell back to `localhost:3000`; robots and sitemap each duplicated a `vercel.app` fallback) — metadata could disagree with robots/sitemap.                                                                                    | FIXED — single `SITE_URL` in `lib/constants.ts` consumed by all three.                                                                                                                                                                    |
| D-21 | Medium   | `next.config.js`                                    | No security headers on any response (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`).                                                                                                                                                                   | FIXED — headers served on every route incl. APIs; smoke-tested. CSP deliberately deferred (needs nonce plumbing for Next inline scripts) — see health-score watch-list.                                                                   |
| D-22 | Medium   | theme-wide (`white/30`, `white/40` text)            | Measured WCAG contrast: `white/40` on this theme = **3.8:1** (fails AA 4.5:1 for normal text) and `white/30` = **2.7:1** (fails even AA-large). Used for information-bearing text: article dates and URLs on cards, the site footer, the error digest, "Run on" labels, empty-result notices. | FIXED — all information-bearing text bumped to `white/50` (**5.1–5.3:1**, AA pass). Decorative icon kept at `white/40` (passes the 3:1 non-text bar); placeholder bumped to `white/40` (supplementary — the input carries an aria-label). |
| D-23 | High     | `package.json` (`next@14.2.15`)                     | `npm audit`: 1 critical + 3 high, incl. GHSA-f82v-jwr5-mffw (middleware authorization bypass) — directly exploitable here to skip the `/api/search` rate limiter.                                                                                                                             | FIXED — upgraded to `next@14.2.35` (latest in the pinned major.minor); remaining advisories require the breaking Next 15/16 major and are triaged in the health-score watch-list.                                                         |
| D-24 | Low      | `app/loading.tsx` (route transitions)               | Navigating to `/history` or `/history/[id]` flashed the root skeleton — the **search page's** hero/search-bar shapes — before the destination rendered.                                                                                                                                       | FIXED — each history route now has its own layout-matched skeleton with screen-reader announcements. `formatRelativeDate` also no longer labels future-dated articles "just now" (unit-tested).                                           |

## Ruled out during audit

- `useSearchParams()` on the home page without a `Suspense` boundary — suspected
  build-breaker, **disproved** by a clean production build (client page bails out
  correctly in Next 14.2).
- Home/history/API `ok`-flag contradictions — none found; every JSON response's
  `ok` agrees with its HTTP status (now locked in by smoke checks).
- Dead nav links — none; every `href` in layout, empty states, error and 404
  pages resolves to a live route.
